import { StatusBar} from "react-native";
import { Text,View,ScrollView,TouchableOpacity,Button } from "react-native";

export default function Employerlogin(){
    return(
        <Text>Welcome to Drive Connect</Text>
    );
}